imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "sftpgo"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold sftpgo config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/sftpgo
      mkdir -p ${MNT_DIR}/config/sftpgo
