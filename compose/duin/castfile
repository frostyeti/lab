imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "duin"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold duin config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/duin
      mkdir -p ${MNT_DIR}/config/duin
