imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "immich"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold immich config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/immich
      mkdir -p ${MNT_DIR}/config/immich
