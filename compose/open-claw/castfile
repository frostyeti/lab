imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "open-claw"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold open-claw config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/open-claw
      mkdir -p ${MNT_DIR}/config/open-claw
