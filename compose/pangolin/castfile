imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "pangolin"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold pangolin dirs"
    uses: shell
    run: |
      mkdir -p ${MNT_DIR}/etc/pangolin
      mkdir -p ${MNT_DIR}/data/pangolin
