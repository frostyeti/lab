# ── Proxy selection ─────────────────────────────────────────────────────────
TRAEFIK_ENABLE=false
CADDY_ENABLE=false

# ── MSSQL ─────────────────────────────────────────────────────────────────────
MSSQL_PID=Developer
# SA password must meet SQL Server complexity requirements (upper+lower+digit+symbol, min 8 chars).
# --special restricts to symbols that SQL Server accepts.
MSSQL_SA_PASSWORD="$(kpv --vault homelab secrets ensure --key default/mssql-sa-password --size 24 --special '!@#%^*')"
