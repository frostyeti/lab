imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "code-server"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold code-server config dirs"
    uses: shell
    run: |
      mkdir -p ${MNT_DIR}/data/code-server
      mkdir -p ${MNT_DIR}/etc/code-server
