imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "meilisearch"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold meilisearch config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/meilisearch
      mkdir -p ${MNT_DIR}/config/meilisearch
