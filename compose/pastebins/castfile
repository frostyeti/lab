imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "pastebins"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold pastebins config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/pastebins
      mkdir -p ${MNT_DIR}/config/pastebins
