# ── Proxy selection ─────────────────────────────────────────────────────────
TRAEFIK_ENABLE=false
CADDY_ENABLE=false

TEMPORAL_HOST=temporal.local
TEMPORAL_UI_HOST=temporal-ui.local
TEMPORAL_PORT=7233
TEMPORAL_UI_PORT=8080
TEMPORAL_DB_NAME=temporal
TEMPORAL_DB_USER=temporal
# Secret — generated and stored in kpv on first use
TEMPORAL_DB_PASSWORD="$(kpv --vault homelab secrets ensure --key default/temporal-db-password --size 32 -S)"
