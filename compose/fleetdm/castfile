imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "fleetdm"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold fleetdm config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/fleetdm
      mkdir -p ${MNT_DIR}/config/fleetdm
