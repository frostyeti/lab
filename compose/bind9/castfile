imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "bind9"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold bind9 config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/bind9
      mkdir -p ${MNT_DIR}/config/bind9
