imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "gatus"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold gatus config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/gatus
      mkdir -p ${MNT_DIR}/config/gatus
