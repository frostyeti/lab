imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "wazuh"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold wazuh config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/wazuh
      mkdir -p ${MNT_DIR}/config/wazuh
