imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "ofelia"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold ofelia config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/ofelia
      mkdir -p ${MNT_DIR}/config/ofelia
