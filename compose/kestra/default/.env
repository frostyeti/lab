# ── Proxy selection ─────────────────────────────────────────────────────────
TRAEFIK_ENABLE=false
CADDY_ENABLE=false

KESTRA_HOST=kestra.local
KESTRA_PORT=8080
KESTRA_DB_NAME=kestra
KESTRA_DB_USER=kestra
# Secret — generated and stored in kpv on first use
KESTRA_DB_PASSWORD="$(kpv --vault homelab secrets ensure --key default/kestra-db-password --size 32 -S)"
