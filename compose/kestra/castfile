imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "kestra"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold kestra dirs"
    uses: shell
    run: |
      mkdir -p ${MNT_DIR}/data/kestra
      mkdir -p ${MNT_DIR}/etc/kestra
