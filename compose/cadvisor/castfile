imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "cadvisor"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold cadvisor config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/cadvisor
      mkdir -p ${MNT_DIR}/config/cadvisor
