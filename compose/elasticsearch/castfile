imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "elasticsearch"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold elasticsearch config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/elasticsearch
      mkdir -p ${MNT_DIR}/config/elasticsearch
