imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "anythingllm"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold anythingllm config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/anythingllm
      mkdir -p ${MNT_DIR}/config/anythingllm
