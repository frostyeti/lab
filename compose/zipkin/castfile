imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "zipkin"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold zipkin config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/zipkin
      mkdir -p ${MNT_DIR}/config/zipkin
