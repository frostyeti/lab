imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "keycloak"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold keycloak config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/keycloak
      mkdir -p ${MNT_DIR}/config/keycloak
