imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "posthog"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold posthog dirs"
    uses: shell
    run: |
      mkdir -p ${MNT_DIR}/data/posthog
      mkdir -p ${MNT_DIR}/etc/posthog
