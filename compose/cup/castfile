imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "cup"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold cup config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/cup
      mkdir -p ${MNT_DIR}/config/cup
