imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "consul"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold consul config dirs"
    uses: shell
    run: |
      mkdir -p ${MNT_DIR}/data/consul
      mkdir -p ${MNT_DIR}/etc/consul
