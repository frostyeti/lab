imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "nextcloud"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold nextcloud config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/nextcloud
      mkdir -p ${MNT_DIR}/config/nextcloud
