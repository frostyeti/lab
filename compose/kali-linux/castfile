imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "kali-linux"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold kali-linux config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/kali-linux
      mkdir -p ${MNT_DIR}/config/kali-linux
