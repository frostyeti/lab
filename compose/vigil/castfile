imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "vigil"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold vigil config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/vigil
      mkdir -p ${MNT_DIR}/config/vigil
