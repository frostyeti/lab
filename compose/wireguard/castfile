imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "wireguard"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold wireguard config dirs"
    uses: shell
    run: |
      mkdir -p ${MNT_DIR}/data/wireguard
      mkdir -p ${MNT_DIR}/etc/wireguard
