imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "nessus"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold nessus config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/nessus
      mkdir -p ${MNT_DIR}/config/nessus
