imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "wikijs"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold wikijs config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/wikijs
      mkdir -p ${MNT_DIR}/config/wikijs
