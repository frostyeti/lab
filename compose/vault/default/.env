# ── Proxy selection ─────────────────────────────────────────────────────────
TRAEFIK_ENABLE=false
CADDY_ENABLE=false

# ── Vault ─────────────────────────────────────────────────────────────────────
VAULT_HOST=vault.local
VAULT_PORT=8200

# Unseal keys — stored in kpv after running: docker exec vault vault operator init
# Then: kpv --vault homelab secrets set --key default/vault-unseal-key-1 --stdin
# These are left blank until vault is initialized; the unseal sidecar exits safely if empty.
VAULT_UNSEAL_KEY_1="$(kpv --vault homelab secrets ensure --key default/vault-unseal-key-1 --size 0 2>/dev/null || echo '')"
VAULT_UNSEAL_KEY_2="$(kpv --vault homelab secrets ensure --key default/vault-unseal-key-2 --size 0 2>/dev/null || echo '')"
VAULT_UNSEAL_KEY_3="$(kpv --vault homelab secrets ensure --key default/vault-unseal-key-3 --size 0 2>/dev/null || echo '')"
