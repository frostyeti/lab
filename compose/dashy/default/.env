# Proxy selection: traefik | caddy | none
PROXY_TYPE=none

# Set to "true" to activate Traefik routing labels
TRAEFIK_ENABLE=false

# Set to the hostname that caddy-docker-proxy should route for this service.
# Leave as "false" to disable caddy routing (caddy ignores non-hostname values).
CADDY_ENABLE=false

# Hostname used by the active proxy
DASHY_HOST=dashy.local

# Direct port binding (used when PROXY_TYPE=none)
DASHY_PORT=4000
