imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "signoz"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold signoz config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/signoz
      mkdir -p ${MNT_DIR}/config/signoz
