imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "graphite"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold graphite config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/graphite
      mkdir -p ${MNT_DIR}/config/graphite
