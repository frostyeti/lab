services:
  peekaping:
    image: ${IMAGE_NAME:-mrzack9999/peekaping}:${TAG:-latest}
    container_name: peekaping
    {{- if ne .env.DEPLOY_MODE "swarm" }}
    restart: unless-stopped
    {{- end }}
    environment:
      - TZ=UTC
    ports:
      - "8080:80"
    volumes:
      - ${MNT_DIR}/data/peekaping:/data
      - ${MNT_DIR}/config/peekaping:/config
    networks:
      - vnet-frontend
      - vnet-backend
    {{- if eq .env.DEPLOY_MODE "swarm" }}
    deploy:
      mode: replicated
      replicas: {{ default "1" .data.replicas }}
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
    {{- end }}

networks:
  vnet-frontend:
    external: true
  vnet-backend:
    external: true
