imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "rustfs"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold rustfs data dirs"
    uses: shell
    run: |
      mkdir -p ${MNT_DIR}/data/rustfs
