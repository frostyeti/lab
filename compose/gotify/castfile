imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "gotify"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold gotify config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/gotify
      mkdir -p ${MNT_DIR}/config/gotify
