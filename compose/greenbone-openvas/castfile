imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "greenbone-openvas"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold greenbone-openvas config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/greenbone-openvas
      mkdir -p ${MNT_DIR}/config/greenbone-openvas
