imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "home-assistant"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold home-assistant config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/home-assistant
      mkdir -p ${MNT_DIR}/config/home-assistant
