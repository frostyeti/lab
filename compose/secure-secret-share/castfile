imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "secure-secret-share"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold secure-secret-share config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/secure-secret-share
      mkdir -p ${MNT_DIR}/config/secure-secret-share
