imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "cloudbeaver"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold cloudbeaver config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/cloudbeaver
      mkdir -p ${MNT_DIR}/config/cloudbeaver
