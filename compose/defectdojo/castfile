imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "defectdojo"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold defectdojo config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/defectdojo
      mkdir -p ${MNT_DIR}/config/defectdojo
