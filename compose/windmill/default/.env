# ── Proxy selection ─────────────────────────────────────────────────────────
TRAEFIK_ENABLE=false
CADDY_ENABLE=false

WINDMILL_HOST=windmill.local
WINDMILL_PORT=8000
WINDMILL_DB_NAME=windmill
WINDMILL_DB_USER=windmill
# Secrets — generated and stored in kpv on first use
WINDMILL_DB_PASSWORD="$(kpv --vault homelab secrets ensure --key default/windmill-db-password --size 32 -S)"
