imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "renovate"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold renovate config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/renovate
      mkdir -p ${MNT_DIR}/config/renovate
