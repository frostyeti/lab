imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "headscale"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold headscale dirs"
    uses: shell
    run: |
      mkdir -p ${MNT_DIR}/data/headscale
      mkdir -p ${MNT_DIR}/etc/headscale
