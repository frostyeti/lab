imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "portainer"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold portainer config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/portainer
      mkdir -p ${MNT_DIR}/config/portainer
