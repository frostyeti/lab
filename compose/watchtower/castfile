imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "watchtower"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold watchtower config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/watchtower
      mkdir -p ${MNT_DIR}/config/watchtower
