imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "arcane"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold arcane config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/arcane
      mkdir -p ${MNT_DIR}/config/arcane
