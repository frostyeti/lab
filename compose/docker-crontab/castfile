imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "docker-crontab"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold docker-crontab config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/docker-crontab
      mkdir -p ${MNT_DIR}/config/docker-crontab
