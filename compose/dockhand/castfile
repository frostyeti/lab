imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "dockhand"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold dockhand config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/dockhand
      mkdir -p ${MNT_DIR}/config/dockhand
