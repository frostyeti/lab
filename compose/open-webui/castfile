imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "open-webui"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold open-webui config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/open-webui
      mkdir -p ${MNT_DIR}/config/open-webui
