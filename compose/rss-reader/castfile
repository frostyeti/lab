imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "rss-reader"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold rss-reader config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/rss-reader
      mkdir -p ${MNT_DIR}/config/rss-reader
