imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "tugtainer"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold tugtainer config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/tugtainer
      mkdir -p ${MNT_DIR}/config/tugtainer
