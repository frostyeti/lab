imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "authentik"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold authentik config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/authentik
      mkdir -p ${MNT_DIR}/config/authentik
