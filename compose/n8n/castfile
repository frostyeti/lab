imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "n8n"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold n8n data dir"
    uses: shell
    run: |
      mkdir -p ${MNT_DIR}/data/n8n
      mkdir -p ${MNT_DIR}/etc/n8n
