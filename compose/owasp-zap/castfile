imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "owasp-zap"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold owasp-zap config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/owasp-zap
      mkdir -p ${MNT_DIR}/config/owasp-zap
