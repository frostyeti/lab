# ── Proxy selection ─────────────────────────────────────────────────────────
TRAEFIK_ENABLE=false
CADDY_ENABLE=false

# Redis runs without a password in dev mode.
# To enable auth in prod, pass --requirepass via command and set:
# REDIS_PASSWORD="$(kpv --vault homelab secrets ensure --key default/redis-password --size 32 -S)"
