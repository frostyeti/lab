imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "chrony"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold chrony config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/chrony
      mkdir -p ${MNT_DIR}/config/chrony
