imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "secure-file-share"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold secure-file-share config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/secure-file-share
      mkdir -p ${MNT_DIR}/config/secure-file-share
