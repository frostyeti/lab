imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "tinyauth"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold tinyauth config dir into .mnt"
    uses: bash
    run: |
      mkdir -p ${MNT_DIR}/data/tinyauth
      mkdir -p ${MNT_DIR}/config/tinyauth
