imports:
  - docker

env:
  MNT_DIR: "${MNT_DIR:-../../.mnt}"
  SERVICE_NAME: "filerun"

inventories:
  - ./prod-inventory.yaml
  - ./test-inventory.yaml

tasks:
  up:before:
    desc: "Scaffold filerun dirs"
    uses: shell
    run: |
      mkdir -p ${MNT_DIR}/data/filerun/html
      mkdir -p ${MNT_DIR}/data/filerun/user-files
